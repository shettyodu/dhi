{
  "name": "dhi-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "DHI Vehicle Passport smart contract (testnet) — deploy-time only, not bundled into serverless functions.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:amoy": "hardhat run scripts/deploy.js --network polygonAmoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
