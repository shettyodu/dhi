{
  "name": "dhi-website",
  "version": "1.0.0",
  "private": true,
  "description": "DHI static storefront + Netlify payment functions (single Netlify site).",
  "scripts": {
    "build:css": "tailwindcss -i tailwind.input.css -o site/assets/tailwind.css --minify",
    "build:index": "node scripts/build-search-index.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "ethers": "^6.13.0",
    "nodemailer": "^6.10.1",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
